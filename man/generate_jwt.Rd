% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_jwt.R
\name{generate_jwt}
\alias{generate_jwt}
\title{Generate a JSON Web Token (JWT) for Apple App Store Connect API}
\usage{
generate_jwt(
  issuer_id,
  key_id,
  private_key_path,
  expiration_time = as.numeric(Sys.time()) + 1200
)
}
\arguments{
\item{issuer_id}{A string representing the Issuer ID obtained from App Store Connect.}

\item{key_id}{A string representing the Key ID associated with the private key.}

\item{private_key_path}{A string specifying the path to the private key file (a \code{.p8} file).}

\item{expiration_time}{A numeric value specifying the token's expiration time as a UNIX timestamp.
Defaults to 20 minutes (1200 seconds) from the current system time.}
}
\value{
A string containing the signed JSON Web Token (JWT).
}
\description{
This function generates a signed JSON Web Token (JWT) using the ES256 algorithm.
The token is required to authenticate requests to Apple's App Store Connect API.
}
\details{
\itemize{
\item The JWT header includes the algorithm (\code{ES256}), key ID (\code{kid}), and type (\code{typ}).
\item The JWT payload includes the issuer (\code{iss}), expiration time (\code{exp}), and audience (\code{aud}).
\item The private key must be readable using the \code{openssl::read_key()} function.
\item The token is signed using \code{jose::jwt_encode_sig()}.
}
}
\examples{
\dontrun{
issuer_id <- "YOUR_ISSUER_ID"
key_id <- "YOUR_KEY_ID"
private_key_path <- "path/to/AuthKey_YOUR_KEY_ID.p8"
jwt <- generate_jwt(issuer_id, key_id, private_key_path)
print(jwt)
}

}
